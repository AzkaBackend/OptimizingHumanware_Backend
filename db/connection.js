const environment = process.env.NODE_ENV || 'development';
// const config = require('../knexfile')[environment];
// module.exports = require('knex')(config);

// Basic connection


const config = {
    host: 'localhost',
    user: 'postgres',
    password: 'Urud@t4bas3',
    database: 'library'
};